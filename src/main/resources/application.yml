
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://database-1.czgzen3ethc7.ap-northeast-2.rds.amazonaws.com/querydsl
    username : hosung
    password : ghtjd114
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace
server:
  port: 8081
